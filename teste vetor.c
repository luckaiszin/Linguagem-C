#include <stdio.h>

int main()
{

    int a1, a2, a3, a4, a5;
    int i;

    for (i = 1; i < 6; i++)
    {
        printf("Digite %d valor", i);
        scanf("%d", &ai);
    }

    printf("Soma: %d", a1 + a2 + a3 + a4 + a5);
    return 0;
}